plugins {
    id 'java'
}

group = 'com.PeacockTeam'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}

jar {
    archiveFileName = 'PeacockUtil.jar'
    manifest {
        attributes 'Main-Class': 'com.PeacockTeam.Main'
    }
}